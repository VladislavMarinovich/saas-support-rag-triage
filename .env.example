# Copy this file to `.env` and fill in the values. `.env` is gitignored — never commit it.

# Anthropic Claude — generates the KB and synthetic tickets.
# Create a key at https://console.anthropic.com  ("API Keys" -> "Create Key")
ANTHROPIC_API_KEY=
