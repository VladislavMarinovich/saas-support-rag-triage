.venv/
/data/
.env
__pycache__/
*.pyc
.ipynb_checkpoints/
.idea/
.DS_Store
# Borrador personal (no publicar) — la versión para el repo se escribe aparte.
docs/mapa-matematicas-polaris.md

# Secretos que NUNCA deben commitearse
# `.dev.vars*` cubre los perfiles por variante (.dev.vars.v1, .dev.vars.v2, …):
# llevan GCP_SA_KEY en claro y son el unico lugar donde LIVE puede valer "true".
# Las plantillas `.example` SI se versionan — son la documentacion del formato.
.dev.vars
.dev.vars.*
!.dev.vars.*.example
polaris-triage-demo-*.json
*-service-account*.json
*serviceaccount*.json

# Cache de embeddings del eval (POL-10): se regenera solo, nunca se versiona
src/eval/cache/
